# ~/.config/mpv/script-opts/webm.conf

# Use H.264 / AAC (standard MP4)
output_format=avc

# Optional: Disable two-pass encoding for faster exports
twopass=no

# Optional: Target quality (CRF: lower is higher quality, 18-23 is good)
crf=20
